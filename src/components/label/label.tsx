import { forwardRef } from 'react';

// ----------------------------------------------------------------------

const Label = forwardRef(
  () => (
    <label />
  ),
);

Label.displayName = 'Label';
export default Label;
