import {
  Container,
} from '@mui/material';
import FavoriteRecipes from '../favoriteRecipes';

export default function FavoriteRecipesView() {
  return (
    <Container>
      <FavoriteRecipes />
    </Container>
  );
}
